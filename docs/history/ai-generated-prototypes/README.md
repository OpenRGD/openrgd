# Removed AI-generated and prototype surfaces

These files and trees were removed from the active repository during the 2026-09-03 hygiene pass.

They remain recoverable from Git history. Their original blob or tree identities are recorded in `INVENTORY.json`.

Removal from the active tree means only that they are not current authority or supported behavior. It does not erase their historical role.

The removed set includes:

- promotional copy that overstated current capabilities;
- onboarding and Docker instructions for commands or artifacts that were not implemented;
- operational and security claims for infrastructure that had not been materialized;
- an unaccepted plugin ABI and permissive plugin loader;
- an untested Time Travel plugin prototype;
- unselected branding proposal files.

The current repository may reintroduce equivalent functionality only through normal evidence, testing and governance. Historical text must not be copied back merely because it existed in the 2025 tree.
