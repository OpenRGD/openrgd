# OpenRGD Repository Reconciliation Audit

**Audit opened:** 2026-09-02  
**Governance freeze recorded:** 2026-09-03  
**Repository:** `OpenRGD/openrgd`  
**Baseline:** `4776e637b4d575d14d55f06423c87cfe1ec0de87` (`main`)  
**Branch:** `reconciliation-canonical-core-v0.1`  
**Governance implementation checkpoint:** `02222e88a4e5f5026d828e3f5d174ae65a0a2428`

## Verdict

```text
TECHNICAL REVIEW READINESS      YES
REPOSITORY-TREE GOVERNANCE      COMPLETE / CI-VALIDATED
MAIN BRANCH PROTECTION          OPEN — ISSUE #2
PULL REQUEST                    OPEN / DRAFT / NOT MERGED
MERGE AUTHORIZATION             NO
RELEASE AUTHORIZATION           NO
```

The branch establishes six executable boundaries:

1. `spec/` is the normative modular source, with verified derived representations in `standard/` and the packaged default seed;
2. `OpenRGD/openrgd` is a non-actuating canonical/tooling root, while physical execution belongs to separately governed runtime and Body Adapter repositories;
3. generated bundles, robot workspaces, exports and unverified external examples are not canonical source;
4. robot-description import produces source-derived Foundation evidence, while seed enrichment remains explicitly compatibility-unverified;
5. `check` and `boot` verify and ground a static profile without claiming runtime readiness or permission to actuate;
6. repository authority, contract maturity, version axes, merge behavior, release tags, evidence exclusions and human accountability are machine-readably frozen.

No commit, tag or release has been written to `main`.

## Verified governance checkpoint

GitHub Actions pull-request workflow:

```text
33735707622
status: completed
conclusion: success
head: 02222e88a4e5f5026d828e3f5d174ae65a0a2428
```

| Check | Result |
|---|---|
| Install on Python 3.10 | PASS |
| Install on Python 3.12 | PASS |
| Dependency consistency | PASS |
| Python source compilation | PASS |
| Repository invariants | PASS |
| Canonical artifact policy | PASS |
| Strict JSON leaf mirror | PASS — 76 expected, 0 missing, 0 mismatched, 0 unexpected |
| Packaged default seed | PASS — 76 expected, 0 missing, 0 mismatched, 0 unexpected |
| Approved seed overrides | PASS — 0 |
| Canonical source-tree profile | PASS — `OPENRGD_SOURCE_TREE_SHA256_V1` |
| Canonical source-tree root | PASS — `sha256:c049dc3fb91c19e668ef0d35b9b40d9052caf1a458b1a108d58b1896189dd5f9` |
| Generated/duplicate artifact absence | PASS |
| Deterministic machine bundle and strict mirror | PASS |
| Runtime prototype quarantine | PASS |
| `rgd run` fail-closed boundary | PASS |
| URDF evidence lifecycle | PASS |
| USDA evidence lifecycle | PASS |
| Static profile inspection | PASS |
| Deterministic ROS 2 static export | PASS |
| Governance validator | PASS |
| Agent Contracts candidate-status validator | PASS |
| SO-101 causal-flow example | PASS |
| Pytest suite | PASS — 35 tests |
| Windows PyInstaller build and upload | PASS |

Checkpoint artifact:

```text
name:   openrgd-rgd-windows
id:     9885693022
size:   12,190,774 bytes
sha256: bb1303549910927f9e1f04c9709fe54ca179ac8404139ab5c534c33073a0d966
```

## Current authority model

| Artifact | Authority | Validation |
|---|---|---|
| `spec/` | Normative modular JSONC source | parsing + canonical source-tree root |
| `standard/` | Derived tracked strict-JSON leaf mirror | parsed equivalence with selected leaves |
| `src/openrgd/seeds/default/spec/` | Derived tracked default profile | byte equality except explicit digest-pinned overrides; currently none |
| `contracts/` | Cross-component interfaces | versioned and maturity-labelled; current Agent Contracts are candidate |
| `governance/` and root policy documents | Repository governance | machine-readable validator + tests |
| `spec/openrgd_unified_spec.json` | Generated untracked machine bundle | deterministic output bound to source root |
| `export/` | Generated untracked static interoperability output | export manifest + source-root commitment |
| `RGD-*` / `my-robots/` | Generated untracked robot workspaces | local profile artifacts, not standard source |
| `tests/fixtures/` | Owned non-normative test evidence | provenance + automated assertions |
| `docs/history/` | Non-normative historical evidence | Git tree/blob identities and archival notes |

## Closure 1 — specification, mirror and seed

Enforced state:

```text
STANDARD: 76 expected, 0 missing, 0 mismatched, 0 unexpected
DEFAULT SEED: 76 expected, 0 missing, 0 mismatched,
0 approved overrides, 0 unexpected
```

`spec/` is the human-readable authority. `standard/` is its strict JSON leaf mirror. The packaged default seed is a derived profile.

A future seed divergence requires an explicit `RUNTIME_PROFILE_OVERRIDE` with a semantic reason, decision reference and both canonical/seed SHA-256 values.

Resolved historical drift:

- ten missing Agency/skill files restored;
- stale seed revisions of kernel header, actuation topology and surface properties replaced by canonical source bytes;
- legacy two-entry skill index archived as historical evidence;
- generated unified products removed from the active seed;
- actual `rgd init` materialization tested;
- project DID personalization limited and followed by rehashing.

## Closure 2 — runtime and safety ownership

The bundled ROS 2/Viam runtime was incomplete and referenced a nonexistent `02_operation/safety_supervisor.jsonc`. It was preserved as historical evidence and removed from the installed package rather than repaired by guessing.

`rgd run` now exposes only a non-actuating status/migration boundary. Legacy runtime targets fail closed with exit code `2` and import no ROS 2, Viam, serial or CAN implementation.

Physical execution belongs to an independent embodied runtime and Body Adapter implementing:

```text
CognitionProposal
→ ActionIntent
→ Somatic Translator
→ CapabilityPlan
→ Operation Safety Gate
→ DecisionTrace
→ Body Adapter
→ hardware
```

No existing safety file was renamed or reinterpreted as the missing module.

## Closure 3 — generated artifacts and canonical hashing

Removed from active authority:

- recursive domain and unified bundles;
- benchmark copies;
- duplicate UR5 workspaces;
- checked-in export products;
- incomplete MSIX material;
- competing generators and parallel CLI code;
- unverified external robot examples.

Original identities remain in Git history and the generated-artifact inventory.

The adopted branch integrity profile is:

```text
OPENRGD_SOURCE_TREE_SHA256_V1
sha256:c049dc3fb91c19e668ef0d35b9b40d9052caf1a458b1a108d58b1896189dd5f9
```

It commits selected source paths, media types, normalized byte counts and exact source SHA-256 values. JSONC comments and formatting are committed. Only the manifest's own integrity value is normalized to `sha256:SELF` while calculating the root.

The root proves content identity under the declared profile. It is not a digital signature and does not prove authorship, review or governance approval.

## Closure 4 — URDF evidence and static ROS 2 lifecycle

The URDF importer emits only:

```text
01_foundation/description.jsonc
01_foundation/actuation_dynamics.jsonc
```

It preserves source-supported links, inertials, topology, units, limits, source dynamics and mimic relations. It does not create kernel, alignment, safety, cognition or HAL policy. Missing values remain absent; malformed and non-finite values fail closed.

A project-owned MIT fixture is maintained under `tests/fixtures/urdf/`.

`rgd alive` records:

```text
profile_kind = SEED_ENRICHED_IMPORTED_EVIDENCE
seed_compatibility_status = UNVERIFIED
```

Static ROS 2 export validates the source and compiled-bundle roots. It emits hardware Xacro only with complete explicit HAL evidence; otherwise it reports `CONFIGURATION_ONLY`. Imported bodies cannot inherit seed HAL through a name collision.

The historical Isaac generator was a placeholder and is not an active target.

## Closure 5 — USDA evidence and static profile inspection

The lightweight USD path is explicitly bounded as:

```text
OPENUSD_USDA_LIGHTWEIGHT_V1
```

It accepts UTF-8 text USDA only and supports directly authored stage metadata plus selected `PhysicsRevoluteJoint` and `PhysicsPrismaticJoint` evidence.

Evidence-bound conversions:

```text
revolute position     degree → radian
prismatic position    stage distance × metersPerUnit → metre
drive effort          SI conversion only with authored
                      metersPerUnit + kilogramsPerUnit
```

Raw source values remain preserved. Missing values remain absent. Non-finite values, inverted limits, duplicate identifiers and ambiguous duplicate attributes fail closed.

`physics:body0` and `physics:body1` remain source relationships; they are not relabelled as universal parent/child links.

The parser does not claim full OpenUSD composition, references, payloads, variants or transform semantics.

A project-owned MIT fixture is maintained under `tests/fixtures/usd/`.

`rgd check` now validates:

- matching source-tree root;
- canonical kernel location;
- identity and non-empty module list;
- safe, unique relative JSONC references;
- module presence;
- JSON object loading without non-finite values.

Its result explicitly states:

```text
physical_execution_assessed = false
runtime_readiness = NOT_ASSESSED
```

`rgd boot` produces:

```text
OPENRGD_NON_ACTUATING_GROUNDING_CONTEXT
```

and always records:

```text
physical_execution.assessed = false
physical_execution.authorized = false
physical_execution.status = NOT_AUTHORIZED_BY_BOOT
```

It does not initialize an embodied runtime or assess the Operation Safety Gate.

## Closure 6 — merge readiness and governance freeze

### Canonical governance

`GOVERNANCE.md` and `governance/policy.json` freeze:

- `OpenRGD/openrgd` as non-actuating canonical/tooling root;
- human accountability for normative acceptance, merge and release;
- temporary AI design profiles as non-authoritative;
- pull-request-only changes to `main`;
- evidence-based normative changes and RFC requirements;
- explicit maturity states;
- fail-closed emergency changes;
- merge commits as the default for preserving granular reviewed history;
- PR #1 as a required merge-commit merge.

### Contract maturity

`contracts/agent/v0.1.0/STATUS.json` freezes:

```text
maturity = candidate
normative = false
accepted = false
stable_release_allowed = false
merge_behavior = PRESERVE_CANDIDATE_STATUS
```

Candidate promotion requires complete normative text, compatibility rules, producer/consumer conformance tests, a validated reference flow, resolution of conflicts and an accepted RFC or equivalent decision.

The validator proves that merge cannot silently promote the package.

### Version and release axes

Future tags are scoped:

```text
standard-vMAJOR.MINOR.PATCH
toolchain-vMAJOR.MINOR.PATCH
contracts-agent-vMAJOR.MINOR.PATCH
```

Existing `v0.1.0` and `v0.1.1` remain historical toolchain/repository evidence and are not moved.

The CI workflow publishes the Windows executable only from `toolchain-v*` tags. A standard or contract tag cannot accidentally publish a toolchain binary.

Merge is not release. PR #1 creates no tag, artifact release, standard promotion or contract promotion.

A future release PR may propose:

```text
Python version: 0.2.0rc1
Git tag:        toolchain-v0.2.0-rc.1
```

### Integrity versus signing

`OPENRGD_SOURCE_TREE_SHA256_V1` remains an integrity commitment, not an authorship signature.

Signing is not represented as implemented. An unsigned release candidate may be published only with explicit disclosure. Stable trust-sensitive releases require a separately implemented signed-tag/release and artifact-provenance lifecycle.

### AI-assisted work

AI systems may assist with drafting, testing, analysis and review. A human maintainer remains accountable for accepting normative decisions and publishing releases. Private chain-of-thought is neither required nor canonical evidence.

## Evidence-scope decision

A checksum record was recovered for:

```text
openrgd-v0.2-aion-ready.zip
sha256 8c8f4a7f9c3ff67504962fb255dd9652e60264538c97fb6a1a037a256d98351d
```

The archive bytes were unavailable in the reconciliation workspace. Therefore:

- contents were not inspected;
- no file or architectural claim was inferred;
- the archive was not used as a source for PR #1;
- it is explicitly excluded and non-blocking;
- later recovery requires digest verification and a separate evidence-delta pull request.

This closes the ambiguity without rewriting history.

## Server-side repository control

At governance freeze time GitHub reported:

```text
main protected: false
repository rulesets: none
```

Repository files cannot enforce this server-side state.

GitHub issue #2 records the required protection:

- pull request required;
- branch up to date;
- required checks:
  - `Validate Python 3.10`
  - `Validate Python 3.12`
  - `Build Windows executable`
- review conversations resolved;
- force pushes blocked;
- `main` deletion blocked;
- zero independent approvals during documented single-maintainer mode;
- one non-author approval after a second maintainer is appointed.

This is the only remaining merge-control gate. PR #1 remains draft until GitHub reports `protected: true` and issue #2 is closed with verification.

## Decision register

The reconciliation decision register currently runs through:

```text
R-001 ... R-049
```

The governance block adds:

- `R-041` human-accountable governance;
- `R-042` explicit single-maintainer mode;
- `R-043` granular commit-history preservation;
- `R-044` scoped release tags;
- `R-045` Agent Contracts remain candidate;
- `R-046` digest-only AION-ready evidence exclusion;
- `R-047` pre-merge branch protection;
- `R-048` integrity/signing separation;
- `R-049` merge/release separation.

## Open work outside reconciliation PR #1

### Generic seed/body compatibility

`UNVERIFIED` remains correct. A generic imported body cannot inherit calibration, physical safety or HAL assertions without a separate compatibility process.

### Full OpenUSD implementation

The text USDA profile is intentionally limited. Full support requires the OpenUSD SDK and explicit ownership of composition, references, payloads, variants, schema APIs and transform semantics.

### Physical validation

Static ROS 2 output has not been validated as a live hardware-bound execution path in this repository. Physical validation belongs to the embodied runtime and Body Adapter repositories.

### Contract promotion

Agent Contracts remain candidate. Promotion is future governance work, not a merge gate for preserving them as candidate material.

### Recovered AION-ready archive

Later recovery is an evidence-delta task, not hidden scope in PR #1.

### Signing and attestations

Signing is a stable-release hardening task, not a merge gate for the historical reconciliation.

## Merge gates

Before PR #1 is merged:

1. apply and verify `main` protection from issue #2;
2. close issue #2 with the GitHub settings evidence;
3. add that verification to PR #1;
4. mark PR #1 ready for review;
5. ensure the branch is current with `main`;
6. confirm the three required checks pass on the final head;
7. complete the public single-maintainer checklist;
8. resolve all conversations;
9. merge through a merge commit;
10. create no tag or release.

## Non-actions

This reconciliation did not:

- modify, merge or force-update `main`;
- create a tag or release;
- rewrite Git history;
- infer the contents of an unavailable archive;
- redistribute removed external examples under a new ownership claim;
- infer HAL bindings from names;
- insert generic drivers or fake physical values;
- claim seed/body compatibility from hashing;
- claim full OpenUSD support;
- launch ROS 2 or actuate hardware;
- promote Agent Contracts to accepted status;
- claim signing that was not implemented;
- expose or require private chain-of-thought.
