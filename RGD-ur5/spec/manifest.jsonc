/**
 * ------------------------------------------------------------------
 * OPEN R.G.D. STANDARD — BUNDLE MANIFEST v0.2
 * ------------------------------------------------------------------
 * FILE: manifest.jsonc
 * PURPOSE:
 *   Top-level descriptor for an OpenRGD bundle.
 *   Declares:
 *     - Which standard version this bundle targets,
 *     - Who authored and maintains it,
 *     - Which domains are present and their maturity.
 *
 * HUMAN INTERPRETATION:
 *   Think of this as the "package.json" of the robot's mind+body.
 *
 * LLM INTERPRETATION:
 *   - Use this file to understand which domains you can rely on
 *     (FOUNDATION / AGENCY / VOLITION / ETHER).
 *   - Do NOT invent new domains. Stick to those declared here.
 */

{
  "meta_group": {
    "bundle_id_str": "bhl_reference_genesis",
    "rgd_standard_version_semver_str": "0.2.0",
    "description_str": "First reference implementation of OpenRGD Standard v0.2.",
    
    "author_str": "OpenRGD Foundation",
    "license_enum": "MIT",
    
    "integrity_hash_str": "sha256:placeholder_auto_generated",
    "created_at_iso8601_str": "2025-11-23T05:10:14.795731Z",

    // Links to core coordination artifacts
    "rgd_schema_ref_str": "rgd_schema.jsonc",
    "kernel_ref_str": "kernel.jsonc"
  },

  // ============================================================
  // DOMAIN REGISTRY
  // Which conceptual layers are present in this bundle?
  // ============================================================
  "domains_map": {
    "01_foundation": {
      "required_bool": true,
      "status_enum": "STABLE"
    },
    "02_operation": {
      "required_bool": true,
      "status_enum": "STABLE"
    },
    "03_agency": {
      "required_bool": true,
      "status_enum": "EXPERIMENTAL"
    },
    "04_volition": {
      "required_bool": false,
      "status_enum": "EXPERIMENTAL"
    },
    "05_evolution": {
      "required_bool": false,
      "status_enum": "PROPOSAL"
    },
    "06_ether": {
      "required_bool": false,
      "status_enum": "PROPOSAL"
    }
  }

  // In future:
  // "files_index_map": { "01_foundation": [...], "03_agency": [...] }
  // could list all filenames if we want stricter tooling.
}
