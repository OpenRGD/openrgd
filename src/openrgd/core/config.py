from rich.console import Console

# Global Console Instance
console = Console()

# App State
state = {
    "quiet": False,
    "verbose": False,
    "cinematic": True,
    "delay": 0.5
}

# Constants
QUOTES = [
    "I think, therefore I ground.",
    "Without definitions, I am just a ghost in the shell.",
    "Wake up, Neo.",
    "Grounding semantic reality..."
]